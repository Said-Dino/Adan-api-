/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/Adan-api-",
    assetPrefix: '/Adan-api-/',
    images:{
        unoptimized: true,
    },
};

export default nextConfig;
